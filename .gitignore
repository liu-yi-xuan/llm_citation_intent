# Secrets / local config (commit the *.example.* templates instead)
config.yml
*keys.json
!*.example.json
.env

# Data — distributed separately (the data/ pointer-README is whitelisted)
data/*
!data/README.md
*.parquet
*.csv
!assets/*.csv

# Generated outputs — no binaries / no PDFs in the repo
*.png
*.pdf
*.svg
*.html

# Python
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
.vscode/
.idea/
